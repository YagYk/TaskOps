# Example remote backend configuration using S3
# Copy this file to backend.tf and uncomment/modify as needed

# terraform {
#   backend "s3" {
#     bucket         = "your-terraform-state-bucket"
#     key            = "taskops/terraform.tfstate"
#     region         = "ap-south-1"
#     encrypt        = true
#     dynamodb_table = "terraform-state-lock"
#   }
# }

# To use this backend:
# 1. Create an S3 bucket for state storage
# 2. Optionally create a DynamoDB table for state locking
# 3. Copy this file to backend.tf
# 4. Update the bucket, key, region, and dynamodb_table values
# 5. Run: terraform init
