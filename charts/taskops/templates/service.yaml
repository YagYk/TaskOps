apiVersion: v1
kind: Service
metadata:
  name: {{ include "taskops.fullname" . }}
  labels:
    {{- include "taskops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "taskops.selectorLabels" . | nindent 4 }}

